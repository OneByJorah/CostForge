{
  "runtime": "Docker Compose",
  "build_method": "docker compose build && docker compose up -d",
  "entry_point": "multi-service (FastAPI backend + nginx frontend)",
  "dependencies": ["docker", "docker compose"],
  "ports": [8000, 8090],
  "deploy_steps": [
    "docker compose config",
    "docker compose build",
    "docker compose up -d"
  ],
  "has_docker": true,
  "has_compose": true,
  "has_npm": false,
  "notes": "Docker daemon is not running on this host; Compose cannot be started here. README will be updated to document this prerequisite."
}